---
phase: 03-fix-home-care-product-dropdown-missing-items
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/services/yclients-goods-catalog.js
autonomous: true
requirements:
  - GOAL-01   # каталог содержит все актуальные товары YClients после первого sync
  - GOAL-03   # sync новых товаров — автоматический (cron вызывает функцию из этого файла; cron-wiring в Wave 4)
  - GOAL-04   # архивация исчезнувших товаров через 24h окно
must_haves:
  truths:
    - "syncGoodsCatalog(salon) полностью реализует bootstrap → enumerate → UPSERT → soft-delete → clearTreeCache"
    - "Функция возвращает структуру { salonId, inserted, updated, archived, goodsSeen, categoriesSeen, errors, durationMs }"
    - "Failed-категории не валят весь sync (per-category try/catch)"
    - "Между YClients-запросами 200ms задержка (D-11)"
    - "is_archived переключается обратно в false при возвращении товара (D-15)"
    - "clearTreeCache(salon.id) вызывается в самом конце успешного sync (D-18)"
  artifacts:
    - path: "backend/services/yclients-goods-catalog.js"
      provides: "Полная реализация syncGoodsCatalog()"
      min_lines: 180
      contains: "syncGoodsCatalog"
  key_links:
    - from: "syncGoodsCatalog"
      to: "ycGet"
      via: "/good_categories/{cid}, /goods/{cid}/{good_id}, /goods/{cid}?category_id=N"
      pattern: "ycGet\\(salon"
    - from: "syncGoodsCatalog"
      to: "yclients_goods_catalog таблица"
      via: "INSERT … ON CONFLICT DO UPDATE RETURNING (xmax = 0) AS is_insert"
      pattern: "INSERT INTO yclients_goods_catalog"
    - from: "syncGoodsCatalog"
      to: "clearTreeCache"
      via: "в самом конце функции перед return"
      pattern: "clearTreeCache\\(salon\\.id\\)"
---

<objective>
Wave 2 — наполнить тело `syncGoodsCatalog(salon)` в `backend/services/yclients-goods-catalog.js` (scaffold создан в Wave 1). Реализовать полный алгоритм из RESEARCH.md §Section 3:

1. Self-guard на отсутствие YClients-кредов (D-context, Pitfall 5).
2. **Bootstrap** списка `category_id`s — opportunistic `/good_categories/{cid}` (D-07), затем reuse уже известных из `yclients_goods_catalog` (D-09), затем fallback через per-good `/goods/{cid}/{good_id}` для distinct `yclients_goods_id` из `goods_sale_items` (D-08).
3. **Enumerate** — для каждого `category_id` paginated `/goods/{cid}?category_id=N&count=200&page=K` до пустой/короткой страницы (D-06), per-page UPSERT с `RETURNING (xmax = 0) AS is_insert` чтобы считать inserted vs updated. Per-category try/catch — failed категория логируется и пропускается, остальные продолжают (D-12). 200ms задержка между страницами (D-11). **Hard cap 50 страниц на категорию** (T4 mitigation).
4. **Soft-delete tail** — UPDATE WHERE last_seen_at < NOW() - INTERVAL '24 hours' AND NOT is_archived (D-13).
5. **Cache invalidation** — `clearTreeCache(salon.id)` (D-18).
6. Вернуть `{ salonId, inserted, updated, archived, goodsSeen, categoriesSeen, errors, durationMs, errorSamples }`.

Endpoint repointing — Wave 3. Cron wiring — Wave 4. Здесь только sync-функция.

Purpose: Это сердце фазы. Без неё каталог пуст и dropdown остаётся пустым. После Wave 2 функцию можно вызвать вручную из Node REPL для одноразового заполнения теста.

Output: Полностью работающий `syncGoodsCatalog(salon)`, идемпотентный, multi-salon-safe, bounded по pages.
</objective>

<execution_context>
@.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-CONTEXT.md
@.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md
@.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-01-SUMMARY.md
</execution_context>

<context>
@CLAUDE.md
@backend/services/yclients-goods-catalog.js
@backend/services/home-care.js
@backend/services/staff.js
@backend/services/yclients.js

<interfaces>
<!-- Wave 1 ввела следующие экспорты — переиспользуем их здесь: -->

From backend/services/yclients-goods-catalog.js (Wave 1 scaffold):
```javascript
const HOME_CARE_CATEGORY_BLACKLIST: Set<string>;       // unused в этом плане (используется в Wave 3)
function isBlacklisted(categoryTitle: string): boolean; // unused здесь
function extractCategoryTitle(g: object, catMap: object): string | null;
async function syncGoodsCatalog(salon): Promise<...>;  // СКАФФОЛД — здесь дописываем тело
```

From backend/services/yclients.js:
```javascript
async function ycGet(salon, endpoint, params): Promise<any>; // 30s timeout, throws on non-success
function clearTreeCache(salonId): void;
```

From backend/db.js: db.query, db.one, db.oneOrNone, db.any, db.many.

From backend/logger.js: createLogger('GoodsCatalog') already initialized in Wave 1 file.
</interfaces>

<schema_reference>
Wave 1 создала таблицу:
```sql
yclients_goods_catalog (
  id BIGSERIAL PK,
  salon_id INT FK salons(id) ON DELETE CASCADE,
  yclients_good_id BIGINT NOT NULL,
  category_id INT,
  category_title VARCHAR(200),
  title VARCHAR(500) NOT NULL DEFAULT '',
  article VARCHAR(200),
  last_seen_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  is_archived BOOLEAN NOT NULL DEFAULT FALSE,
  created_at, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (salon_id, yclients_good_id)
)
```
</schema_reference>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Реализовать syncGoodsCatalog(salon) — bootstrap + enumerate + upsert + archive + cache invalidation</name>
  <files>backend/services/yclients-goods-catalog.js</files>
  <read_first>
    - backend/services/yclients-goods-catalog.js (текущий scaffold из Wave 1 — заменяем тело syncGoodsCatalog, остальные экспорты сохраняем)
    - backend/services/home-care.js целиком (паттерн pagination + delay + per-cat error handling — строки 18-35)
    - backend/services/staff.js строки 91-127 (UPSERT с RETURNING (xmax = 0) AS is_insert — это тот самый трюк, верифицированный в RESEARCH §Section 3)
    - backend/services/yclients.js строки 32-60 (ycGet — обёртка над axios, throws на не-success)
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md §Section 2 строки 399-433 (UPSERT pattern), §Section 3 строки 434-548 (полный псевдокод алгоритма + RETURNING optimization), §Section 8 (failure handling)
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-CONTEXT.md D-06..D-15 — все sync-decisions
  </read_first>
  <action>
    Заменить тело функции `syncGoodsCatalog(salon)` (сейчас scaffold-stub) на полную имплементацию ниже. Не трогать `HOME_CARE_CATEGORY_BLACKLIST`, `isBlacklisted`, `extractCategoryTitle`, `module.exports` из Wave 1 — оставляем их без изменений.

    **Полная замена тела `syncGoodsCatalog`:**

    ```javascript
    async function syncGoodsCatalog(salon) {
      // Self-guard (D-context Pitfall 5, matches services/home-care.js:9 pattern)
      if (!salon || !salon.yclients_company_id || !salon.yclients_user_token) {
        return { skipped: true, reason: 'no-yclients' };
      }
      const cid = salon.yclients_company_id;
      const salonId = salon.id;
      const startedAt = Date.now();
      const errors = [];
      const PAGE_SIZE = 200;
      const MAX_PAGES_PER_CATEGORY = 50;        // T-03-04 hard cap (DoS guard)
      const PACE_MS = 200;                       // D-11
      const BOOTSTRAP_PACE_MS = 150;             // matches services/staff.js:58
      let inserted = 0, updated = 0, goodsSeen = 0;

      logger.info(`Salon ${salonId}: starting catalog sync (cid=${cid})`);

      // ─── STEP A — Bootstrap category id list (D-07, D-08, D-09) ─────────
      const catMap = {};        // cat_id → cat_title (best-effort, used by extractCategoryTitle)
      const catIds = new Set();

      // A1. Try /good_categories — opportunistic, may 404 (D-07)
      try {
        const cats = await ycGet(salon, `/good_categories/${cid}`);
        if (Array.isArray(cats)) {
          for (const c of cats) {
            if (c && c.id != null && c.title) {
              catMap[c.id] = c.title;
              catIds.add(c.id);
            }
          }
          logger.info(`Salon ${salonId}: /good_categories returned ${catIds.size} categories`);
        }
      } catch (e) {
        logger.warn(`Salon ${salonId}: /good_categories failed (${e.message}); will fallback`);
      }

      // A2. Reuse known cat_ids from previous syncs (D-09)
      if (catIds.size === 0) {
        const known = await db.any(
          `SELECT DISTINCT category_id FROM yclients_goods_catalog
            WHERE salon_id = $1 AND category_id IS NOT NULL`,
          [salonId]
        );
        for (const r of known) catIds.add(r.category_id);
        if (catIds.size > 0) {
          logger.info(`Salon ${salonId}: reused ${catIds.size} cat_ids from existing catalog`);
        }
      }

      // A3. Bootstrap from goods_sale_items via per-good /goods/{cid}/{good_id} (D-08)
      // NOTE: goods_sale_items has NO numeric category_id column (verified by mcp__postgres__query
      // before planning); must hit per-good endpoint to discover cat_ids on first-ever sync.
      if (catIds.size === 0) {
        const sold = await db.any(
          `SELECT DISTINCT gsi.yclients_goods_id AS gid
             FROM goods_sale_items gsi
             JOIN goods_sales gs ON gs.id = gsi.sale_id
            WHERE gs.salon_id = $1 AND gsi.yclients_goods_id IS NOT NULL`,
          [salonId]
        );
        logger.info(`Salon ${salonId}: bootstrapping cat_ids from ${sold.length} sold goods`);
        for (let i = 0; i < sold.length; i++) {
          const gid = sold[i].gid;
          try {
            const data = await ycGet(salon, `/goods/${cid}/${gid}`);
            if (data && data.category_id != null) catIds.add(data.category_id);
            if (data && data.category && typeof data.category === 'object' && data.category.title && data.category_id != null) {
              catMap[data.category_id] = data.category.title;
            }
          } catch (e) {
            errors.push({ step: 'bootstrap', goodId: gid, msg: e.message });
          }
          await new Promise(r => setTimeout(r, BOOTSTRAP_PACE_MS));
        }
      }

      // A4. If still nothing — bail; cron will retry (D-19 + D-12)
      if (catIds.size === 0) {
        logger.warn(`Salon ${salonId}: no category_ids discovered; skipping`);
        return { skipped: true, reason: 'no-categories', errors: errors.length, errorSamples: errors.slice(0, 5) };
      }

      // ─── STEP B — Enumerate goods per category (D-06, D-11, D-12) ───────
      const catIdList = [...catIds];
      logger.info(`Salon ${salonId}: enumerating ${catIdList.length} categories`);

      for (const catId of catIdList) {
        try {
          let page = 1;
          while (page <= MAX_PAGES_PER_CATEGORY) {        // T-03-04 hard cap
            const goods = await ycGet(salon, `/goods/${cid}`, { category_id: catId, count: PAGE_SIZE, page });
            if (!Array.isArray(goods) || goods.length === 0) break;
            for (const g of goods) {
              if (g.good_id == null) continue;            // D-10 — same gotcha as bug fix in Wave 1
              const categoryTitle = extractCategoryTitle(g, catMap);
              const result = await db.one(`
                INSERT INTO yclients_goods_catalog
                  (salon_id, yclients_good_id, category_id, category_title, title, article, last_seen_at, is_archived)
                VALUES ($1, $2, $3, $4, $5, $6, NOW(), FALSE)
                ON CONFLICT (salon_id, yclients_good_id) DO UPDATE
                  SET category_id    = EXCLUDED.category_id,
                      category_title = EXCLUDED.category_title,
                      title          = EXCLUDED.title,
                      article        = EXCLUDED.article,
                      last_seen_at   = NOW(),
                      is_archived    = FALSE,
                      updated_at     = NOW()
                RETURNING (xmax = 0) AS is_insert
              `, [
                salonId,
                g.good_id,
                g.category_id != null ? g.category_id : null,
                categoryTitle,
                String(g.title || '').slice(0, 500),
                g.article != null ? String(g.article).slice(0, 200) : null,
              ]);
              if (result.is_insert) inserted++; else updated++;
              goodsSeen++;
            }
            if (goods.length < PAGE_SIZE) break;
            page++;
            await new Promise(r => setTimeout(r, PACE_MS));   // D-11
          }
          if (page > MAX_PAGES_PER_CATEGORY) {
            logger.warn(`Salon ${salonId}: category ${catId} hit MAX_PAGES_PER_CATEGORY=${MAX_PAGES_PER_CATEGORY} cap`);
          }
        } catch (e) {
          errors.push({ step: 'enumerate', catId, msg: e.message });
          logger.warn(`Salon ${salonId}: category ${catId} failed: ${e.message}`);
          // continue to next category (D-12 — partial-failure resilience)
        }
      }

      // ─── STEP C — Soft-delete stale (D-13) ───────────────────────────────
      const archived = await db.query(`
        UPDATE yclients_goods_catalog
           SET is_archived = TRUE, updated_at = NOW()
         WHERE salon_id = $1
           AND last_seen_at < NOW() - INTERVAL '24 hours'
           AND NOT is_archived
        RETURNING id
      `, [salonId]);

      // ─── STEP D — Invalidate tree cache (D-18) ──────────────────────────
      clearTreeCache(salonId);

      const durationMs = Date.now() - startedAt;
      logger.info(
        `Salon ${salonId}: catalog sync done. ` +
        `inserted=${inserted} updated=${updated} archived=${archived.rowCount} ` +
        `goodsSeen=${goodsSeen} categories=${catIdList.length} errors=${errors.length} duration=${durationMs}ms`
      );

      return {
        salonId,
        inserted,
        updated,
        archived: archived.rowCount,
        goodsSeen,
        categoriesSeen: catIdList.length,
        errors: errors.length,
        errorSamples: errors.slice(0, 5),
        durationMs,
      };
    }
    ```

    Жесткие требования:
    - НЕ менять `module.exports` (4 имени из Wave 1 сохраняются).
    - НЕ удалять/переименовывать `extractCategoryTitle`, `isBlacklisted`, `HOME_CARE_CATEGORY_BLACKLIST`.
    - НЕ добавлять новых зависимостей в `require(...)` помимо тех, что уже импортированы в Wave 1 + ничего нового тут не нужно.
    - НЕ использовать `db.one` где может прийти 0 строк — для UPSERT с RETURNING (xmax=0) одна строка гарантирована конструкцией; для optional category lookups использовать `db.oneOrNone`.
    - НЕ логировать целый объект `salon` (T-03-03 — может содержать tokens). Логировать только `salon.id` и `cid`.
    - НЕ ждать на этапе bootstrap слишком агрессивно: задержка 150ms перед каждым per-good запросом (D-11 spirit — 100-200ms между запросами).
    - Использовать `String(g.title || '').slice(0, 500)` — защита от non-string и от overflow VARCHAR(500).
  </action>
  <verify>
    <automated>cd /root/loyalpro && node -e "
const m = require('./backend/services/yclients-goods-catalog');
(async () => {
  // Self-guard test
  const r1 = await m.syncGoodsCatalog({});
  console.log('no-creds:', JSON.stringify(r1));
  if (!r1.skipped || r1.reason !== 'no-yclients') process.exit(1);

  const r2 = await m.syncGoodsCatalog({ id: 999, yclients_company_id: 0, yclients_user_token: '' });
  console.log('falsy-creds:', JSON.stringify(r2));
  if (!r2.skipped) process.exit(1);

  console.log('OK self-guard');
})();
" && grep -c "INSERT INTO yclients_goods_catalog" backend/services/yclients-goods-catalog.js && grep -c "ON CONFLICT (salon_id, yclients_good_id)" backend/services/yclients-goods-catalog.js && grep -c "RETURNING (xmax = 0)" backend/services/yclients-goods-catalog.js && grep -c "INTERVAL '24 hours'" backend/services/yclients-goods-catalog.js && grep -c "clearTreeCache(salonId)" backend/services/yclients-goods-catalog.js && grep -c "MAX_PAGES_PER_CATEGORY" backend/services/yclients-goods-catalog.js</automated>
  </verify>
  <done>
    Тело `syncGoodsCatalog` полностью реализовано: self-guard на отсутствие YClients-кредов; bootstrap category_ids через 3-уровневый fallback (`/good_categories` → reuse-known → per-good `/goods/{cid}/{gid}`); paginated enumeration `/goods/{cid}?category_id=N&page=K` с per-page UPSERT (`ON CONFLICT … DO UPDATE … RETURNING (xmax = 0)` для inserted vs updated counting), per-category try/catch, hard cap `MAX_PAGES_PER_CATEGORY=50`, 200ms задержка между страницами; soft-delete tail (`UPDATE … WHERE last_seen_at < NOW() - INTERVAL '24 hours'`); `clearTreeCache(salonId)` в самом конце; возврат `{salonId, inserted, updated, archived, goodsSeen, categoriesSeen, errors, errorSamples, durationMs}`. Скаффолд-стаб из Wave 1 заменён, но `module.exports`, `extractCategoryTitle`, `isBlacklisted`, `HOME_CARE_CATEGORY_BLACKLIST` сохранены без изменений. Логи содержат только `salon.id` и `cid` — не объект `salon` целиком (T-03-W2-01 mitigation). Multi-salon scope соблюдается во всех SQL-запросах через `salon_id = $1`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 2 scope)

| Boundary | Description |
|----------|-------------|
| Cron / one-shot caller → `syncGoodsCatalog` | Internal call; `salon` объект приходит из `salons` table — доверенный источник, но содержит `yclients_user_token` (не должен утечь в логи). |
| Backend → YClients API | Outbound HTTPS через `ycGet`. Bearer-токен берётся из `salon.yclients_user_token`. Ответы от YClients — untrusted: `title`, `category.title`, `article` могут быть произвольными строками. |
| Backend → Postgres | Параметризованные запросы (`db.one`, `db.any`, `db.query`). User-input (через YClients ответ) идёт ТОЛЬКО как параметры. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W2-01 | Information Disclosure (token leak in logs) | `syncGoodsCatalog` logger calls | mitigate | `[GoodsCatalog]` logger пишет ТОЛЬКО `salon.id` и `cid` — никогда не объект `salon` целиком (он содержит `yclients_user_token`). Все error-объекты в `errors[]` логируются как `{step, catId, msg}` — где `msg = e.message`, не `e.stack` (стек-трейс может содержать config/headers). Acceptance: `grep -E "logger\\.(info\\|warn\\|error)\\(.*salon[^.]" backend/services/yclients-goods-catalog.js \| wc -l` === 0 (никаких лог-вызовов с `salon` без `.id` сразу после). |
| T-03-W2-02 | Information Disclosure (cross-salon cache leak) | `clearTreeCache(salonId)` | mitigate | `_treeCache` в `services/yclients.js` per-salonId by design (key = salonId). `syncGoodsCatalog` вызывает `clearTreeCache(salon.id)` (НЕ `clearTreeCache()` без аргумента). Acceptance: `grep -c "clearTreeCache(salonId)" backend/services/yclients-goods-catalog.js` === 1; `grep -c "clearTreeCache()" backend/services/yclients-goods-catalog.js` === 0. Каждый сalon очищает только свой локальный кеш. |
| T-03-W2-03 | Denial of Service (runaway pagination) | enumerate loop | mitigate | Hard cap `MAX_PAGES_PER_CATEGORY = 50`. При page=51 цикл выходит, warn-лог пишется, sync продолжается. 50 × 200 = 10 000 товаров на категорию — далеко выше любого реалистичного сценария (PERI CLINIC ≈ 30 товаров/категория). Acceptance: `grep -c "MAX_PAGES_PER_CATEGORY" backend/services/yclients-goods-catalog.js` ≥ 2 (объявление + проверка в while). |
| T-03-W2-04 | Tampering / SQL injection (untrusted YClients payload) | UPSERT в `yclients_goods_catalog` | mitigate | Все SQL-параметры идут через `$1..$6` плейсхолдеры (db.one). YClients-payload поля проходят `String(g.title \|\| '').slice(0, 500)` и `g.article != null ? String(g.article).slice(0, 200) : null` — длина ограничена под VARCHAR(500)/VARCHAR(200), тип нормализован к строке. Никакой конкатенации в SQL-строку. Acceptance: `grep -c "\\\${" backend/services/yclients-goods-catalog.js` (template-literal в SQL) === 0 для строк, начинающихся `INSERT/UPDATE/SELECT`. |
| T-03-W2-05 | Denial of Service (YClients quota burn) | bootstrap A3 + enumerate | mitigate | (a) `PACE_MS = 200` между paginated `/goods/{cid}` запросами (D-11); (b) `BOOTSTRAP_PACE_MS = 150` между per-good `/goods/{cid}/{gid}` bootstrap-запросами (matches `services/staff.js:58`). Worst-case PERI CLINIC bootstrap: ~150 sold goods × 150ms = 22.5s. Per-cat enumerate: 26 cat × 1-2 page × 200ms = ~10s. Total ~30s — far below YClients RPM cap. Acceptance: `grep -c "setTimeout" backend/services/yclients-goods-catalog.js` ≥ 2 (один в bootstrap, один в enumerate). |
| T-03-W2-06 | Repudiation | Audit | accept | `[GoodsCatalog]` logger пишет start + done с counts (inserted/updated/archived/errors/duration). Adequate audit trail для PERI CLINIC scale (no SOC2). |
| T-03-W2-07 | Elevation of Privilege | n/a | n/a | Wave 2 не вводит endpoints. Не применимо. |
</threat_model>

<verification>
После завершения Task 1:

1. **Lint/syntax**:
   ```
   node -e "require('./backend/services/yclients-goods-catalog')"
   ```
   Expected: успешный require без ошибок.

2. **Self-guard работает (offline-проверка, без БД)**:
   ```
   node -e "const m = require('./backend/services/yclients-goods-catalog'); m.syncGoodsCatalog({}).then(r => console.log(JSON.stringify(r)))"
   ```
   Expected: `{"skipped":true,"reason":"no-yclients"}`.

3. **Структурные grep-проверки на тело функции**:
   - `grep -c "INSERT INTO yclients_goods_catalog" backend/services/yclients-goods-catalog.js` === 1
   - `grep -c "ON CONFLICT (salon_id, yclients_good_id)" backend/services/yclients-goods-catalog.js` === 1
   - `grep -c "RETURNING (xmax = 0)" backend/services/yclients-goods-catalog.js` === 1
   - `grep -c "INTERVAL '24 hours'" backend/services/yclients-goods-catalog.js` === 1
   - `grep -c "clearTreeCache(salonId)" backend/services/yclients-goods-catalog.js` === 1
   - `grep -c "MAX_PAGES_PER_CATEGORY" backend/services/yclients-goods-catalog.js` ≥ 2 (объявление + while-условие)
   - `grep -c "PACE_MS" backend/services/yclients-goods-catalog.js` ≥ 2 (объявление + setTimeout)
   - `grep -c "BOOTSTRAP_PACE_MS" backend/services/yclients-goods-catalog.js` ≥ 2

4. **Multi-salon scoping (CLAUDE.md hard rule)**:
   ```
   grep -E "FROM yclients_goods_catalog\|UPDATE yclients_goods_catalog\|INTO yclients_goods_catalog" backend/services/yclients-goods-catalog.js
   ```
   Каждое попадание должно сопровождаться `salon_id = $1` (или эквивалентом) в том же SQL-блоке.

5. **Token leak guard (T-03-W2-01)**:
   ```
   grep -nE "logger\\.(info|warn|error)\\(\`?.*\\\${salon[^.]" backend/services/yclients-goods-catalog.js
   ```
   Expected: 0 совпадений (все логи используют `${salonId}` или `${salon.id}`, не `${salon}` целиком).

6. **Опционально (требует БД и YClients-кредов)** — ручной dry-run на тестовом стенде из Node REPL:
   ```
   node -e "
   const { db } = require('./backend/db');
   const { syncGoodsCatalog } = require('./backend/services/yclients-goods-catalog');
   db.one('SELECT * FROM salons WHERE id=1').then(s => syncGoodsCatalog(s)).then(r => console.log(JSON.stringify({salonId: r.salonId, inserted: r.inserted, categoriesSeen: r.categoriesSeen, errors: r.errors, durationMs: r.durationMs})))
   "
   ```
   Expected (после первого запуска на пустой таблице): `inserted ≥ 240, categoriesSeen ≥ 25, errors === 0`. Этот шаг — pre-flight для Wave 4 ручного cutover, не обязателен для acceptance Wave 2.

7. **Идемпотентность (если запустить второй раз подряд)**:
   `inserted === 0, updated === goodsSeen` — потому что UNIQUE conflict path заглатывает все строки.
</verification>

<success_criteria>
- [ ] `syncGoodsCatalog(salon)` полностью реализована (тело заменено, scaffold-stub удалён)
- [ ] Self-guard на отсутствие YClients-кредов возвращает `{skipped:true, reason:'no-yclients'}` без сетевых вызовов
- [ ] Bootstrap category_ids работает по 3-уровневому fallback: `/good_categories` (D-07) → reuse-known (D-09) → per-good `/goods/{cid}/{gid}` (D-08)
- [ ] Enumerate per-category читает `/goods/{cid}?category_id=N&count=200&page=K` до пустой страницы (D-06)
- [ ] Per-page UPSERT использует `ON CONFLICT (salon_id, yclients_good_id) DO UPDATE … RETURNING (xmax = 0) AS is_insert` для inserted vs updated counting
- [ ] Per-category try/catch (D-12) — failed категория логируется и пропускается, остальные продолжают
- [ ] Hard cap `MAX_PAGES_PER_CATEGORY = 50` соблюдается в while-цикле (T-03-W2-03)
- [ ] Между страницами — `PACE_MS = 200` задержка (D-11); между bootstrap per-good — `BOOTSTRAP_PACE_MS = 150`
- [ ] Soft-delete tail (D-13): `UPDATE … SET is_archived = TRUE WHERE last_seen_at < NOW() - INTERVAL '24 hours' AND NOT is_archived RETURNING id`
- [ ] `is_archived = FALSE` восстанавливается через `EXCLUDED` clause при возвращении товара (D-15)
- [ ] `clearTreeCache(salonId)` вызывается в самом конце успешного sync (D-18)
- [ ] Возврат: `{salonId, inserted, updated, archived, goodsSeen, categoriesSeen, errors, errorSamples, durationMs}`
- [ ] `module.exports` сохранён без изменений (4 имени из Wave 1)
- [ ] `extractCategoryTitle`, `isBlacklisted`, `HOME_CARE_CATEGORY_BLACKLIST` не тронуты
- [ ] Логи содержат только `salon.id` и `cid` — никогда не объект `salon` целиком (T-03-W2-01)
- [ ] Все SQL-запросы скоупятся через `salon_id = $1` (multi-salon правило CLAUDE.md)
- [ ] Все SQL-параметры идут через плейсхолдеры `$1..$N` — никакой конкатенации в SQL (T-03-W2-04)
- [ ] threat_model — все 7 threats документированы и dispositioned
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-02-SUMMARY.md` documenting:
- Files modified (1): `backend/services/yclients-goods-catalog.js` (тело `syncGoodsCatalog` дописано, ~180 строк добавлено)
- Diff summary: scaffold-stub из Wave 1 заменён на полную реализацию; экспорты сохранены
- Self-guard verified offline: `syncGoodsCatalog({})` → `{skipped:true, reason:'no-yclients'}` без сетевых вызовов
- Структурные grep-чеки пройдены (INSERT INTO, ON CONFLICT, RETURNING (xmax = 0), INTERVAL '24 hours', clearTreeCache(salonId), MAX_PAGES_PER_CATEGORY)
- Token leak guard verified: ни один logger-call не дампит объект `salon` целиком
- Optional dry-run результат (если выполнен на тестовом стенде): salon_id=1 → inserted/categoriesSeen/errors/durationMs
- Outstanding for Wave 3: переписать `/api/home-care/products` и `/api/home-care/product-tree` на чтение из `yclients_goods_catalog` + добавить юнит-тесты
- Outstanding for Wave 4: подключить `syncGoodsCatalog` в cron-handler `'0 */3 * * *'` + ручной production cutover
</output>
