---
phase: 03-fix-home-care-product-dropdown-missing-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations.js
  - backend/services/yclients-goods-catalog.js
  - backend/services/home-care.js
autonomous: true
requirements:
  - GOAL-04   # архивирование исчезнувших товаров (schema поддерживает is_archived + last_seen_at)
  - GOAL-07   # bug fix g.id → g.good_id в bulk-цикле syncGoodsCategories
must_haves:
  truths:
    - "Таблица yclients_goods_catalog существует в БД с правильной схемой и индексами"
    - "Файл backend/services/yclients-goods-catalog.js существует и экспортирует syncGoodsCatalog, HOME_CARE_CATEGORY_BLACKLIST, isBlacklisted, extractCategoryTitle"
    - "Bulk-цикл в backend/services/home-care.js использует g.good_id (не g.id)"
  artifacts:
    - path: "backend/migrations.js"
      provides: "CREATE TABLE IF NOT EXISTS yclients_goods_catalog + 2 индекса"
      contains: "yclients_goods_catalog"
    - path: "backend/services/yclients-goods-catalog.js"
      provides: "Scaffold для syncGoodsCatalog и blacklist-хелперов"
      exports: ["syncGoodsCatalog", "HOME_CARE_CATEGORY_BLACKLIST", "isBlacklisted", "extractCategoryTitle"]
    - path: "backend/services/home-care.js"
      provides: "Исправленный bulk-цикл, использующий g.good_id"
      contains: "g.good_id"
  key_links:
    - from: "backend/services/yclients-goods-catalog.js"
      to: "backend/db.js"
      via: "require('../db')"
      pattern: "require.*\\./db"
    - from: "backend/services/yclients-goods-catalog.js"
      to: "backend/services/yclients.js"
      via: "ycGet, clearTreeCache imports"
      pattern: "require.*services/yclients"
---

<objective>
Wave 1 — параллельная подготовка фундамента: (a) добавить миграцию для таблицы `yclients_goods_catalog` и индексов, (b) создать scaffold нового сервис-файла `services/yclients-goods-catalog.js` с сигнатурами и константами (без логики sync — только blacklist + утилиты + пустая `syncGoodsCatalog`), (c) починить one-character баг в `services/home-care.js:25-29` (`g.id` → `g.good_id`, D-10).

Все три задачи независимы по файлам и могут выполняться параллельно.

Purpose: Каждая последующая Wave опирается на эти артефакты — Wave 2 заполняет тело `syncGoodsCatalog`, Wave 3 переписывает endpoints с использованием blacklist-хелперов и читает таблицу, Wave 4 подключает sync в cron. Wave 1 не вносит функциональных изменений в продакшн (миграция создаёт пустую таблицу, scaffold не вызывается, баг-фикс ускоряет существующий sync без изменения внешнего поведения).

Output: Новая таблица `yclients_goods_catalog` в БД (после рестарта сервера или ручного применения миграции), новый файл `backend/services/yclients-goods-catalog.js`, починенный bulk-цикл в существующем `home-care.js`.
</objective>

<execution_context>
@.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-CONTEXT.md
@.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md
</execution_context>

<context>
@CLAUDE.md
@backend/migrations.js
@backend/services/home-care.js
@backend/services/staff.js
@backend/services/yclients.js

<interfaces>
<!-- Контракты, которые экспортирует существующий код и которые нам нужны: -->

From backend/services/yclients.js:
```javascript
async function ycGet(salon, endpoint, params = {}): Promise<any>
function getTreeCache(salonId): { products?, services?, ts? } | null
function setTreeCache(salonId, key, data): void
function clearTreeCache(salonId): void
```

From backend/db.js:
```javascript
const db = {
  query: (sql, params) => Promise<{ rows, rowCount }>,
  one: (sql, params) => Promise<row>,        // throws if no row
  oneOrNone: (sql, params) => Promise<row|null>,
  any: (sql, params) => Promise<row[]>,
  many: (sql, params) => Promise<row[]>      // throws if empty
};
```

From backend/logger.js:
```javascript
function createLogger(name: string): { info, warn, error, debug }
```

From backend/migrations.js:
- runMigrations(client) функция последовательно вызывает client.query(...).catch(() => {}) с CREATE/ALTER IF NOT EXISTS блоками. Новые блоки добавляются перед `}` на строке 313. Pattern: каждый CREATE TABLE / CREATE INDEX отдельным await client.query(...).catch(() => {}).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить миграцию yclients_goods_catalog + 2 индекса</name>
  <files>backend/migrations.js</files>
  <read_first>
    - backend/migrations.js (строки 270-313 — паттерн создания portfolio_categories + portfolio_items, особенно завершающий стиль)
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md §Section 1 — Schema (строки 360-397) — точный SQL таблицы и индексов
  </read_first>
  <action>
    Перед закрывающей `}` функции `runMigrations` (строка 313 — сразу после блока `idx_portfolio_items_staff` на строках 309-312) вставить ровно три блока `await client.query(...).catch(() => {})`. Стилистика и отступы совпадают с соседними блоками portfolio_*.

    Реализует D-13 (soft-delete), D-14 (никогда не делаем hard-delete — поэтому boolean `is_archived`, не `archived_at`), частично D-01..D-05 (создаёт хранилище, читаемое endpoints в Wave 3).

    Скопировать дословно (вместе с комментариями):

    ```javascript
      // ── YClients goods catalog (Phase 03 — full product catalog from YClients) ──
      await client.query(`
        CREATE TABLE IF NOT EXISTS yclients_goods_catalog (
          id                BIGSERIAL PRIMARY KEY,
          salon_id          INTEGER     NOT NULL REFERENCES salons(id) ON DELETE CASCADE,
          yclients_good_id  BIGINT      NOT NULL,
          category_id       INTEGER,
          category_title    VARCHAR(200),
          title             VARCHAR(500) NOT NULL DEFAULT '',
          article           VARCHAR(200),
          last_seen_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
          is_archived       BOOLEAN     NOT NULL DEFAULT FALSE,
          created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
          updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
          UNIQUE (salon_id, yclients_good_id)
        )
      `).catch(() => {});

      await client.query(`
        CREATE INDEX IF NOT EXISTS idx_ygc_salon_active_cat
          ON yclients_goods_catalog (salon_id, category_title)
          WHERE NOT is_archived
      `).catch(() => {});

      await client.query(`
        CREATE INDEX IF NOT EXISTS idx_ygc_salon_last_seen
          ON yclients_goods_catalog (salon_id, last_seen_at)
      `).catch(() => {});
    ```

    Не использовать отдельный .sql-файл (запрещено в проекте). Не менять существующие миграции. Не использовать DROP/ALTER ... DROP — только IF NOT EXISTS / NOT EXISTS паттерны. ON DELETE CASCADE на salon_id обязателен (мульти-салон правило из CLAUDE.md).
  </action>
  <verify>
    <automated>cd /root/loyalpro && grep -n "yclients_goods_catalog" backend/migrations.js | head -5 && grep -n "idx_ygc_salon_active_cat\|idx_ygc_salon_last_seen" backend/migrations.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "yclients_goods_catalog" backend/migrations.js` ≥ 4 (CREATE TABLE + 2 индекса + UNIQUE строка)
    - `grep "CREATE TABLE IF NOT EXISTS yclients_goods_catalog" backend/migrations.js` находит ровно 1 совпадение
    - `grep "idx_ygc_salon_active_cat" backend/migrations.js` находит 1 совпадение и содержит `WHERE NOT is_archived`
    - `grep "idx_ygc_salon_last_seen" backend/migrations.js` находит 1 совпадение
    - `grep "REFERENCES salons(id) ON DELETE CASCADE" backend/migrations.js | grep -c yclients_goods` ≥ 0 (присутствует FK constraint в новом блоке — проверить контекстно)
    - После рестарта сервера (или ручного применения через `mcp__postgres__query`) команда `SELECT to_regclass('yclients_goods_catalog')` возвращает не-null значение
  </acceptance_criteria>
  <done>
    Миграция добавлена в правильное место (строки ~313, перед закрывающей `}`), синтаксически корректна, не ломает существующие миграции, использует только safe-pattern.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold backend/services/yclients-goods-catalog.js (blacklist + сигнатуры, без логики sync)</name>
  <files>backend/services/yclients-goods-catalog.js</files>
  <read_first>
    - backend/services/home-care.js (целиком — образец стиля сервис-файла + текущая 3-веточная нормализация category на строках 27-29 — для extractCategoryTitle)
    - backend/services/yclients.js (строки 15-32, 200-208 — экспорты ycGet, clearTreeCache)
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md §Section 4 (строки 549-572) — точный текст blacklist + isBlacklisted; §Pitfall 3 (строки 252-266) — extractCategoryTitle 3-branch helper
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-CONTEXT.md D-03..D-04 — точный список 6 категорий blacklist
  </read_first>
  <action>
    Создать новый файл `backend/services/yclients-goods-catalog.js`. Это **scaffold** — реализуем константы и pure-helpers; функция `syncGoodsCatalog` объявляется с правильной сигнатурой и пустым телом, возвращающим placeholder. Полная имплементация sync — в Wave 2 (Plan 02).

    Реализует D-03 (точные 6 категорий blacklist), D-04 (lower+trim сравнение), D-15 (`is_archived=false` восстановление — ниже в схеме upsert, реализуется в Wave 2). Pure-функции (blacklist + extractCategoryTitle) ниже покрываются юнит-тестами в Wave 3 — но сейчас уже должны быть реализованы и экспортированы.

    Содержимое файла — скопировать дословно:

    ```javascript
    // ============================================================
    // YClients Goods Catalog Sync (Phase 03)
    // Persistent mirror of YClients goods catalog per-salon.
    // Source of truth for /api/home-care/products and /product-tree
    // dropdowns. Replaces sales-history-derived dropdown.
    // ============================================================
    const { db } = require('../db');
    const { ycGet, clearTreeCache } = require('./yclients');
    const { createLogger } = require('../logger');
    const logger = createLogger('GoodsCatalog');

    // ── Blacklist of YClients category titles hidden from home-care dropdown ──
    // (D-03 from CONTEXT.md — phase 03)
    // Comparison is case-insensitive on lower(trim(title)) (D-04).
    // To change the list, edit this array — it's a single source of truth.
    const HOME_CARE_CATEGORY_BLACKLIST = new Set([
      'расходники',
      'канцелярия',
      'препараты',
      'аптека',
      'сертификаты сеть peri clinic',
      'абонементы сеть peri clinic',
    ]);

    /**
     * Returns true if a category title belongs to the blacklist.
     * Empty/null titles return false ("Без категории" passes through and may
     * be filtered upstream if needed).
     * @param {string|null|undefined} categoryTitle
     * @returns {boolean}
     */
    function isBlacklisted(categoryTitle) {
      if (!categoryTitle || typeof categoryTitle !== 'string') return false;
      return HOME_CARE_CATEGORY_BLACKLIST.has(categoryTitle.trim().toLowerCase());
    }

    /**
     * Normalize YClients per-good response into a category title string.
     * YClients returns category in 3 shapes: object {id, title}, string, or
     * null with category_id populated separately. Mirrors the 3-branch
     * pattern in services/home-care.js:27-29.
     * @param {object} g          - YClients good payload
     * @param {Record<number,string>} catMap - cat_id → cat_title fallback map
     * @returns {string|null}
     */
    function extractCategoryTitle(g, catMap) {
      if (g && g.category && typeof g.category === 'object' && g.category.title) {
        return g.category.title;
      }
      if (g && typeof g.category === 'string') return g.category;
      if (g && g.category_id != null && catMap && catMap[g.category_id]) {
        return catMap[g.category_id];
      }
      return null;
    }

    /**
     * Sync the full YClients goods catalog for a salon into yclients_goods_catalog.
     * Wave 2 (Plan 02) implements the body: bootstrap → enumerate per category →
     * UPSERT → soft-delete tail → clearTreeCache.
     *
     * @param {object} salon - salons row (must have id, yclients_company_id, yclients_user_token)
     * @returns {Promise<{
     *   skipped?: boolean, reason?: string,
     *   salonId?: number, inserted?: number, updated?: number, archived?: number,
     *   goodsSeen?: number, categoriesSeen?: number, errors?: number, durationMs?: number
     * }>}
     */
    async function syncGoodsCatalog(salon) {
      // SCAFFOLD ONLY — body implemented in Wave 2 (Plan 02).
      // Self-guard against missing creds (matches syncGoodsCategories line 9):
      if (!salon || !salon.yclients_company_id || !salon.yclients_user_token) {
        return { skipped: true, reason: 'no-yclients' };
      }
      logger.warn(`syncGoodsCatalog: scaffold called for salon ${salon.id} — body not yet implemented`);
      return { skipped: true, reason: 'not-implemented' };
    }

    module.exports = {
      syncGoodsCatalog,
      HOME_CARE_CATEGORY_BLACKLIST,
      isBlacklisted,
      extractCategoryTitle,
    };
    ```

    Не добавлять никакой sync-логики — это scaffold. Не реэкспортировать tree-cache хелперы (Open Question #3 в RESEARCH.md — рекомендовано NO). Не вызывать `syncGoodsCatalog` из других файлов в этой Wave (cron-вызов добавляется в Wave 4).
  </action>
  <verify>
    <automated>cd /root/loyalpro && node -e "const m = require('./backend/services/yclients-goods-catalog'); console.log('exports:', Object.keys(m).join(',')); console.log('blacklist size:', m.HOME_CARE_CATEGORY_BLACKLIST.size); console.log('isBlacklisted Расходники:', m.isBlacklisted('Расходники')); console.log('isBlacklisted Forlled:', m.isBlacklisted('Forlled')); console.log('extractCategoryTitle obj:', m.extractCategoryTitle({category:{title:'X'}},{})); console.log('extractCategoryTitle str:', m.extractCategoryTitle({category:'Y'},{})); console.log('extractCategoryTitle id:', m.extractCategoryTitle({category_id:5},{5:'Z'}));"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `backend/services/yclients-goods-catalog.js` существует
    - `node -e "const m = require('./backend/services/yclients-goods-catalog'); …"` печатает все 4 экспорта: `syncGoodsCatalog,HOME_CARE_CATEGORY_BLACKLIST,isBlacklisted,extractCategoryTitle`
    - `m.HOME_CARE_CATEGORY_BLACKLIST.size === 6`
    - `m.isBlacklisted('Расходники') === true`, `m.isBlacklisted('РАСХОДНИКИ ') === true` (case+whitespace), `m.isBlacklisted('Forlled') === false`, `m.isBlacklisted(null) === false`, `m.isBlacklisted('') === false`
    - `m.extractCategoryTitle({category:{title:'A'}},{}) === 'A'`
    - `m.extractCategoryTitle({category:'B'},{}) === 'B'`
    - `m.extractCategoryTitle({category_id:5},{5:'C'}) === 'C'`
    - `m.extractCategoryTitle({},{}) === null`
    - `await m.syncGoodsCatalog({})` возвращает `{skipped:true, reason:'no-yclients'}` (self-guard работает)
    - `grep -c "HOME_CARE_CATEGORY_BLACKLIST" backend/services/yclients-goods-catalog.js` ≥ 2 (объявление + export)
    - `grep "расходники\|канцелярия\|препараты\|аптека\|сертификаты сеть peri clinic\|абонементы сеть peri clinic" backend/services/yclients-goods-catalog.js | wc -l` === 6 (все 6 категорий присутствуют)
  </acceptance_criteria>
  <done>
    Файл создан, четыре экспорта работают, scaffold-функция возвращает known placeholder, blacklist содержит 6 заявленных категорий, helpers корректны на всех 3 ветках category-shape.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bug fix — g.id → g.good_id в bulk-цикле syncGoodsCategories</name>
  <files>backend/services/home-care.js</files>
  <read_first>
    - backend/services/home-care.js (строки 18-35, особенно 25-29 — место бага)
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-RESEARCH.md §Section 5 (строки 645-672) — точный diff
    - .planning/phases/03-fix-home-care-product-dropdown-missing-items/03-CONTEXT.md D-10 — обоснование
  </read_first>
  <action>
    Реализует **D-10**: bulk-loop `syncGoodsCategories` в `backend/services/home-care.js` использует `g.id` (всегда null в YClients API), фильтрует все строки и силится на медленный fallback. Замена `g.id` на `g.good_id` восстанавливает bulk-path.

    Это **single-character fix в трёх соседних строках** (одна замена в условии + три замены в ключах map). Не трогать остальную логику файла. Не трогать fallback в строке 50 (`goodCatMap[goodId]` — `goodId` приходит из `goods_sale_items.yclients_goods_id`, уже правильное).

    Исходный фрагмент (строки 25-29):

    ```javascript
          for (const g of goods) {
            if (g.id == null) continue;
            if (g.category && typeof g.category === 'object' && g.category.title) goodCatMap[g.id] = g.category.title;
            else if (g.category && typeof g.category === 'string') goodCatMap[g.id] = g.category;
            else if (g.category_id != null && catMap[g.category_id]) goodCatMap[g.id] = catMap[g.category_id];
          }
    ```

    Заменить на:

    ```javascript
          for (const g of goods) {
            if (g.good_id == null) continue;
            if (g.category && typeof g.category === 'object' && g.category.title) goodCatMap[g.good_id] = g.category.title;
            else if (g.category && typeof g.category === 'string') goodCatMap[g.good_id] = g.category;
            else if (g.category_id != null && catMap[g.category_id]) goodCatMap[g.good_id] = catMap[g.category_id];
          }
    ```

    Только эти 4 строки внутри блока `for (const g of goods)`. Никаких других изменений в файле.
  </action>
  <verify>
    <automated>cd /root/loyalpro && grep -n "g\.good_id\|g\.id ==" backend/services/home-care.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "g\.good_id" backend/services/home-care.js` === 4 (одно условие + три ключа map)
    - `grep -c "g\.id == null" backend/services/home-care.js` === 0 (баг устранён)
    - `grep -c "goodCatMap\[g\.id\]" backend/services/home-care.js` === 0
    - `grep -c "goodCatMap\[g\.good_id\]" backend/services/home-care.js` === 3
    - `node -e "require('./backend/services/home-care')"` загружается без ошибок (синтаксически валидно)
    - `git diff backend/services/home-care.js | grep -E "^[-+]" | grep -v "^[+-]{3}" | wc -l` === 8 (4 строки удалены + 4 добавлены — точечный fix)
  </acceptance_criteria>
  <done>
    Только 4 строки в bulk-цикле изменены, fallback path в строке 50 не тронут, файл синтаксически валиден, экспорты `syncGoodsCategories`, `getTreeCache`, `setTreeCache`, `clearTreeCache` сохранены.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 1 scope only)

| Boundary | Description |
|----------|-------------|
| Server startup → DB | runMigrations выполняется один раз при boot; DDL контролируется кодом, не извне |
| Admin/specialist webapp → backend (NOT yet — endpoints в Wave 3) | Здесь только schema + scaffold; нет доступа извне в этой Wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `migrations.js` блок | mitigate | Все DDL в `IF NOT EXISTS` обёртке + `.catch(() => {})` — нельзя случайно удалить existing данные. ON DELETE CASCADE только на `salon_id` (правильное cleanup поведение при удалении салона). |
| T-03-02 | Information Disclosure | Cross-salon | mitigate | UNIQUE `(salon_id, yclients_good_id)` гарантирует, что одинаковый YClients good в двух салонах хранится двумя строками. Запросы из Wave 3 будут фильтровать по `salon_id` (отдельный threat в Plan 03). |
| T-03-03 | Denial of Service | Schema | accept | BIGSERIAL id безопасен (overflow @ 9e18). Без CHECK constraint на title (slice(0,500) на write — Wave 2). |
| T-03-04 | Elevation of Privilege | n/a | n/a | Wave 1 не открывает endpoints. Не применимо. |
| T-03-05 | Repudiation | Audit | accept | `created_at`, `updated_at`, `last_seen_at` присутствуют — adequate audit trail для admin-UI. |
</threat_model>

<verification>
После завершения всех трёх задач:

1. Перезапустить сервер локально (или применить миграцию через `mcp__postgres__query`) и проверить:

```sql
SELECT to_regclass('yclients_goods_catalog') AS exists;
-- Expected: 'yclients_goods_catalog' (not null)

SELECT indexname FROM pg_indexes
WHERE tablename = 'yclients_goods_catalog' AND schemaname = 'public';
-- Expected: yclients_goods_catalog_pkey, yclients_goods_catalog_salon_id_yclients_good_id_key,
--           idx_ygc_salon_active_cat, idx_ygc_salon_last_seen

SELECT COUNT(*) FROM yclients_goods_catalog;
-- Expected: 0 (таблица только что создана, пустая)
```

2. Sanity check на новый сервис-файл:
```
node -e "const m = require('./backend/services/yclients-goods-catalog'); console.log(m.isBlacklisted('Препараты'), m.isBlacklisted('Forlled'))"
# Expected: true false
```

3. Существующий sync продолжает работать без регрессов:
```
grep "g\.good_id\|goodCatMap\[g\.good_id\]" backend/services/home-care.js | wc -l   # Expected: 4
```
</verification>

<success_criteria>
- [ ] Миграция yclients_goods_catalog добавлена в `backend/migrations.js` перед закрытием `runMigrations`
- [ ] Таблица `yclients_goods_catalog` существует в БД после применения миграции (проверка через `to_regclass`)
- [ ] Два индекса созданы: `idx_ygc_salon_active_cat` (partial WHERE NOT is_archived), `idx_ygc_salon_last_seen`
- [ ] Файл `backend/services/yclients-goods-catalog.js` создан и экспортирует ровно 4 имени
- [ ] `HOME_CARE_CATEGORY_BLACKLIST` содержит 6 категорий из D-03
- [ ] `isBlacklisted()` корректен на 4 case-сценариях (lower, trim, null, mixed-case)
- [ ] `extractCategoryTitle()` корректен на всех 3 branches (object/string/id+map) + null fallback
- [ ] `syncGoodsCatalog({})` возвращает `{skipped:true, reason:'no-yclients'}` (self-guard)
- [ ] `backend/services/home-care.js` использует `g.good_id` (не `g.id`) в bulk-цикле
- [ ] Никаких других изменений в `home-care.js` (export-сигнатуры неизменны, fallback-path не тронут)
- [ ] Все три задачи независимы по файлам — зависимостей внутри Wave нет
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-home-care-product-dropdown-missing-items/03-01-SUMMARY.md` documenting:
- Files modified (3): migrations.js, home-care.js, new yclients-goods-catalog.js
- Migration verified applied (то регкласс exists)
- Bug fix line count and file diff size
- Outstanding for Wave 2: implement syncGoodsCatalog body
</output>
